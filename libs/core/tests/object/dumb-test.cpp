#include <xdev.hpp>
#include <gtest/gtest.h>
#include <iostream>

using namespace xdev;
using namespace std;

TEST(TestObject, Base) {
    /*auto test = XObject::Create<XObject>();
    test->listen("propExample", [] (auto value) {
       cout << value << endl;
    });*/
}
